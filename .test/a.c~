
#include <iostream>

#include "boost/numeric/interval.hpp"
using boost::numeric::interval;
using namespace boost::numeric;

int main() {

	interval<double> x(2,3), y(-55, -50);

	std::cout << lower(x*y) << ", " << upper(x*y << std::endl;

	return 0;
}
